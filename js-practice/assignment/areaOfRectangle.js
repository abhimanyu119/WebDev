// Area of Rectangle
function areaOfRectangle(length, width) {
    return length * width;
}
var length = 10;
var width = 20;
console.log(`Area of Rectangle with length ${length} and width ${width} is ${areaOfRectangle(length, width)}`)